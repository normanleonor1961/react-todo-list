import React from 'react'

export default function TodoBody({todos}) {
  return todos.map((todo, index)) => {
    <div id='task'
        key={index}>

        </div>
  }
  
  (
    <div>TodoBody</div>
  )
}
